server {
  listen 80;
  server_name usc-scheduler.com www.usc-scheduler.com;

  root /usr/share/nginx/html;
  index index.html;

  # ACME challenge for Let's Encrypt
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  # Serve SPA
  location / {
    try_files $uri /index.html;
  }

  # Proxy API to Node service
  location /api/ {
    proxy_pass http://api:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # SSE support
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    proxy_buffering off;
    add_header X-Accel-Buffering no;
  }

  sendfile on;
}

  # SSL server block (enable after certs are issued)
  # server {
  #   listen 443 ssl http2;
  #   server_name usc-scheduler.com www.usc-scheduler.com;
  #
  #   ssl_certificate /etc/letsencrypt/live/usc-scheduler.com/fullchain.pem;
  #   ssl_certificate_key /etc/letsencrypt/live/usc-scheduler.com/privkey.pem;
  #   ssl_trusted_certificate /etc/letsencrypt/live/usc-scheduler.com/chain.pem;
  #
  #   root /usr/share/nginx/html;
  #   index index.html;
  #
  #   location / {
  #     try_files $uri /index.html;
  #   }
  #
  #   location /api/ {
  #     proxy_pass http://api:3001;
  #     proxy_http_version 1.1;
  #     proxy_set_header Host $host;
  #     proxy_set_header X-Real-IP $remote_addr;
  #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #     proxy_set_header X-Forwarded-Proto $scheme;
  #
  #     proxy_read_timeout 3600s;
  #     proxy_send_timeout 3600s;
  #     proxy_buffering off;
  #     add_header X-Accel-Buffering no;
  #   }
  # }
